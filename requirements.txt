# EmailParse V1.0 Dependencies

# Core dependencies
pyyaml>=6.0
requests>=2.31.0
rich>=13.0.0

# Testing
pytest>=7.4.0
pytest-mock>=3.11.0

# Email handling (imaplib is built-in to Python)
# Will use Python's built-in imaplib module

# Optional: For better email parsing
email-validator>=2.0.0